# PS02 转接板 CPPI V3 指令解析

> 基于截图抓包数据，参考 `G:\DeviceLink\docs\PS02` 下的协议文档进行逐条解析。

---

## 一、协议基础（CPPI V3.1 帧格式）

截图中的所有指令均采用 **CPPI V3.1** 协议，帧结构如下：

| 字段 | 长度 | 说明 |
|------|------|------|
| 帧头 | 1B | 固定 `0x55` |
| 控制字段 | 1B | bit0: 1=发送帧/0=响应帧；bit[1:3]=001 表示 CPPI V3.1 |
| 目标地址 | 3B | 小端，设备ID << 8 + 端口号 |
| 源地址 | 3B | 小端，设备ID << 8 + 端口号 |
| 功能码 | 2B | 小端，决定指令功能 |
| 流水号 | 1B | 用于匹配请求/响应 |
| 数据长度 N | 2B | 小端，数据内容的字节数 |
| 头部校验 | 1B | CRC8，校验帧头到数据长度字段（共13字节） |
| 数据内容 | NB | 第一字节为响应错误码（仅响应帧），后续为实际数据 |
| 数据校验 | 2B | CRC16，校验数据段；无数据时为 `0xFFFF` |

**固定通信参数：**
- PC（主机）源地址：`0x112236`
- 转接板（从机）目标地址：`0x000123`
- 转接板地址：`0x0001`（PS02标准板卡默认地址）

**错误码定义：**

| 错误码 | 含义 |
|--------|------|
| 0x00 | 无错误 |
| 100 | CRC 校验错误 |
| 101 | 无此指令 |
| 102 | 当前状态不支持此操作 |
| 103 | 密码错误 |
| 104 | 参数格式错误 |
| 105 | 参数超范围 |
| 106 | 执行错误 |
| 107 | 参数错误 |

---

## 二、功能码定义（截图涉及）

| 功能码 | 名称 | 说明 | 来源文档 |
|--------|------|------|----------|
| `0x0106` | 读取设备固件版本 | 返回 ASCII 字符串（Max 32B） | CPPI V3功能码定义规范 |
| `0x0108` | 读取设备硬件版本 | 返回 ASCII 字符串（Max 32B） | CPPI V3功能码定义规范 |
| `0x0210` | 设定输出项目 | 参数：项目代号(1B) + 输出值类型(1B) | PS02标准板卡-串口指令集 |
| `0x0300` | 扫描从设备 | 请求无参数；返回扫描结果(1B) | PS02标准板卡-串口指令集 |
| `0x0301` | 读取参数（推测） | 请求无数据；返回1B参数值 | 未在文档中明确定义，推测为内部调试指令 |

**0x0210 设定输出项目的参数说明：**
- 项目代号：`0`=关闭所有档位，`1`=MaOut（电流输出），`2`=VOut（电压输出）
- 输出值类型：`0`=输出零点，`1`=输出满量程

**0x0300 扫描从设备的返回值：**

- `0`：未连接设备
- `1`：连接设备为 OWI 电流接口
- `2`：连接设备为 OWI 电压接口
- `3`：连接设备为 485 接口

---

## 三、逐条指令解析

### 指令 1：扫描从设备

| | 原始数据 |
|---|---|
| **发送** | `55 03 23 01 00 36 22 11 00 03 01 01 00 D1 01 D1 F1` |
| **接收** | `55 02 36 22 11 23 01 00 00 03 01 01 00 67 00 F0 E1` |

**发送帧解析：**

| 字段 | 值 | 说明 |
|------|-----|------|
| 帧头 | `0x55` | 固定帧头 |
| 控制字段 | `0x03` | 发送帧 + CPPI V3.1 |
| 目标地址 | `0x000123` | 转接板地址 |
| 源地址 | `0x112236` | PC 地址 |
| 功能码 | `0x0300` | **扫描从设备** |
| 流水号 | `0x01` | 第1条指令 |
| 数据长度 | `0x0001` | 1字节 |
| 头部校验 | `0xD1` | CRC8 |
| 数据内容 | `0x01` | 扫描参数 |
| 数据校验 | `0xD1F1` | CRC16 |

**接收帧解析：**

| 字段 | 值 | 说明 |
|------|-----|------|
| 帧头 | `0x55` | 固定帧头 |
| 控制字段 | `0x02` | 响应帧 + CPPI V3.1 |
| 目标地址 | `0x112236` | PC 地址 |
| 源地址 | `0x000123` | 转接板地址 |
| 功能码 | `0x0300` | **扫描从设备** |
| 流水号 | `0x01` | 匹配请求 |
| 数据长度 | `0x0001` | 1字节 |
| 头部校验 | `0x67` | CRC8 |
| 数据内容 | `0x00` | 错误码=0（成功） |
| 数据校验 | `0xF0E1` | CRC16 |

**意义：** PC 通过转接板扫描下游从设备的接口类型，转接板响应成功（错误码 0x00）。

---

### 指令 2：读取设备固件版本

| | 原始数据 |
|---|---|
| **发送** | `55 03 23 01 00 36 22 11 06 01 02 00 00 1E FF FF` |
| **接收** | `55 02 36 22 11 23 01 00 06 01 02 14 00 AB 00 41 32 30 2D 39 38 20 56 30 30 2E 30 30 2E 30 30 2E 30 37 AA B7` |

**发送帧解析：**

| 字段 | 值 | 说明 |
|------|-----|------|
| 功能码 | `0x0106` | **读取设备固件版本** |
| 流水号 | `0x02` | 第2条指令 |
| 数据长度 | `0x0000` | 无请求数据 |
| 数据校验 | `0xFFFF` | 无数据时固定值 |

**接收帧解析：**

| 字段 | 值 | 说明 |
|------|-----|------|
| 数据长度 | `0x0014` | 20字节 |
| 数据内容 | `00 41 32 30 2D 39 38 20 56 30 30 2E 30 30 2E 30 30 2E 30 37` | 错误码(1B) + 版本字符串(19B) |
| 错误码 | `0x00` | 成功 |
| 版本字符串 | `A20-98 V00.00.00.07` | ASCII 解码 |
| 数据校验 | `0xAAB7` | CRC16 |

**意义：** 读取转接板固件版本，返回 `A20-98 V00.00.00.07`。

---

### 指令 4：读取设备硬件版本

| | 原始数据 |
|---|---|
| **发送** | `55 03 23 01 00 36 22 11 08 01 04 00 00 31 FF FF` |
| **接收** | `55 02 36 22 11 23 01 00 08 01 04 0E 00 51 00 41 32 30 2D 39 38 20 56 30 30 2E 30 31 CE CA` |

**发送帧解析：**

| 字段 | 值 | 说明 |
|------|-----|------|
| 功能码 | `0x0108` | **读取设备硬件版本** |
| 流水号 | `0x04` | 第4条指令 |
| 数据长度 | `0x0000` | 无请求数据 |

**接收帧解析：**

| 字段 | 值 | 说明 |
|------|-----|------|
| 数据长度 | `0x000E` | 14字节 |
| 数据内容 | `00 41 32 30 2D 39 38 20 56 30 30 2E 30 31` | 错误码(1B) + 版本字符串(13B) |
| 错误码 | `0x00` | 成功 |
| 版本字符串 | `A20-98 V00.01` | ASCII 解码 |
| 数据校验 | `0xCECA` | CRC16 |

**意义：** 读取转接板硬件版本，返回 `A20-98 V00.01`。

---

### 指令 6：扫描从设备（第二次）

| | 原始数据 |
|---|---|
| **发送** | `55 03 23 01 00 36 22 11 00 03 06 01 00 C7 00 F0 E1` |
| **接收** | `55 02 36 22 11 23 01 00 00 03 06 01 00 71 00 F0 E1` |

**解析：** 功能码 `0x0300`（扫描从设备），流水号 `0x06`。与指令 1 相同的操作，第二次扫描从设备。

> 注意：此请求帧数据长度为 0（无数据），数据校验为 `0xF0E1`（非 `0xFFFF`），与协议文档描述不完全一致，可能是实现差异。

---

### 指令 12：扫描从设备（第三次）

| | 原始数据 |
|---|---|
| **发送** | `55 03 23 01 00 36 22 11 00 03 0C 01 00 40 01 D1 F1` |
| **接收** | `55 02 36 22 11 23 01 00 00 03 0C 01 00 F6 00 F0 E1` |

**解析：** 功能码 `0x0300`（扫描从设备），流水号 `0x0C`，请求数据 `0x01`。与指令 1 相同的操作，第三次扫描从设备。

### 指令 13：Modbus RTU 转发 — 启用 OWI 通信模式（补充）

| | 原始数据 |
|---|---|
| **发送** | `55 03 26 01 00 36 22 11 00 04 0D 0B 00 A8 01 29 80 00 00 01 02 00 01 25 26 53 24` |
| **接收** | `55 02 36 22 11 26 01 00 00 04 0D 09 00 08 00 01 29 80 00 00 01 B4 0C CE EA` |

**发送帧解析：**

| 字段 | 值 | 说明 |
|------|-----|------|
| 帧头 | `0x55` | 固定帧头 |
| 控制字段 | `0x03` | 发送帧 + CPPI V3.1 |
| 目标地址 | `0x000126` | 设备ID=0x0001, **端口号=0x26(38)** |
| 源地址 | `0x112236` | PC 地址 |
| 功能码 | `0x0400` | **Modbus RTU 转发指令**（推测） |
| 流水号 | `0x0D` | 第13条指令 |
| 数据长度 | `0x000B` | 11字节 |
| 头部校验 | `0xA8` | CRC8 |
| 数据校验 | `0x5324` | CRC16 |

**发送数据内容解析（Modbus RTU 报文）：**

数据内容为一条完整的 Modbus RTU 指令，通过转接板转发给下游变送器：

| 字段 | 值 | 说明 |
|------|-----|------|
| Modbus 从机地址 | `0x01` | 下游变送器地址 |
| Modbus 功能码 | `0x29` | **F41 写寄存器** |
| 起始寄存器地址 | `0x8000` | **OWI通信使能**寄存器 |
| 寄存器数量 | `0x0001` | 写入1个寄存器 |
| 数据长度 | `0x02` | 2字节 |
| 寄存器值 | `0x0001` | **启用 OWI 通信模式** |
| CRC16 | `0x2526` | Modbus RTU 校验 |

**接收帧解析：**

| 字段 | 值 | 说明 |
|------|-----|------|
| 帧头 | `0x55` | 固定帧头 |
| 控制字段 | `0x02` | 响应帧 + CPPI V3.1 |
| 目标地址 | `0x112236` | PC 地址 |
| 源地址 | `0x000126` | 转接板端口38 |
| 功能码 | `0x0400` | Modbus RTU 转发 |
| 流水号 | `0x0D` | 匹配请求 |
| 数据长度 | `0x0009` | 9字节 |
| 头部校验 | `0x00` | CRC8 |
| 数据校验 | `0xCEEA` | CRC16 |

**接收数据内容解析：**

| 字段 | 值 | 说明 |
|------|-----|------|
| CPPI 错误码 | `0x00` | 成功 |
| Modbus 响应长度 | `0x08` | 8字节 Modbus RTU 响应 |
| Modbus 从机地址 | `0x01` | 下游变送器地址 |
| Modbus 功能码 | `0x29` | **F41 写寄存器**（确认） |
| 起始寄存器地址 | `0x8000` | OWI通信使能寄存器 |
| 寄存器数量 | `0x0001` | 写入1个寄存器 |
| Modbus CRC16 | `0x0CB4` | Modbus RTU 校验 |

**意义：** PC 通过转接板（端口38），以 Modbus RTU 协议向下游变送器（地址0x01）写入寄存器 0x8000 = 0x0001，**启用 OWI 通信模式**。变送器响应成功，确认 OWI 通信模式已启用。

> **寄存器 0x8000 说明**（来自 PS02通信协议）：系统上电默认为 DAC 工作模式，配置为 `0x0001` 则进入 OWI 通信模式，此时可进行 OWI 读写通信，DAC 被占用；配置为 `0x0000` 则退出 OWI 通信模式，回到 DAC 工作模式。

**与前12条指令的关键区别：**
- 目标地址从 `0x000123`（端口35）变为 `0x000126`（端口38），说明转接板连接了不同端口的下游设备
- 功能码 `0x0400` 为 Modbus RTU 透传指令，数据内容是完整的 Modbus RTU 报文
- 这条指令是 PC → 转接板 → 变送器 的三级通信，转接板充当协议网关

---

## 四、通信时序总结

```
PC (0x112236)                                    转接板 (0x000123)
    |                                                  |
    |--- 扫描从设备 (0x0300, seq=01) ------------------>|
    |<-- 响应成功 --------------------------------------|
    |                                                  |
    |--- 读取固件版本 (0x0106, seq=02) ---------------->|
    |<-- A20-98 V00.00.00.07 --------------------------|
    |                                                  |
    |--- 读取固件版本 (0x0106, seq=03) ---------------->|
    |<-- A20-98 V00.00.00.07 (重复确认) ----------------|
    |                                                  |
    |--- 读取硬件版本 (0x0108, seq=04) ---------------->|
    |<-- A20-98 V00.01 --------------------------------|
    |                                                  |
    |--- 关闭所有输出 (0x0210, seq=05) ---------------->|
    |<-- 响应成功 --------------------------------------|
    |                                                  |
    |--- 扫描从设备 (0x0300, seq=06) ------------------>|
    |<-- 响应成功 --------------------------------------|
    |                                                  |
    |--- 读取参数#1~#5 (0x0301, seq=07~0B) ----------->|
    |<-- 0x47/0x00/0x6B/0xD6/0xBD --------------------|
    |                                                  |
    |--- 扫描从设备 (0x0300, seq=0C) ------------------>|
    |<-- 响应成功 --------------------------------------|
    |                                                  |
    |                                              转接板 (0x000126, 端口38)
    |                                                  |
    |--- Modbus转发:启用OWI (0x0400, seq=0D) --------->|
    |<-- 变送器写入成功 --------------------------------|
```

## 五、指令功能分类汇总

| 类别 | 功能码 | 指令名称 | 调用次数 | 作用 |
|------|--------|----------|----------|------|
| **设备识别** | `0x0106` | 读取设备固件版本 | 2次 | 获取转接板固件版本号 |
| **设备识别** | `0x0108` | 读取设备硬件版本 | 1次 | 获取转接板硬件版本号 |
| **设备管理** | `0x0300` | 扫描从设备 | 3次 | 扫描下游设备接口类型 |
| **输出控制** | `0x0210` | 设定输出项目 | 1次 | 关闭所有电流/电压输出 |
| **参数读取** | `0x0301` | 读取内部参数 | 5次 | 读取转接板内部状态参数 |
| **协议网关** | `0x0400` | Modbus RTU 转发 | 1次 | 透传 Modbus RTU 报文至下游变送器 |

## 六、关键发现

1. **通信协议：** PC 与转接板之间使用 CPPI V3.1 协议，所有多字节数据采用小端模式。
2. **设备信息：** 转接板型号为 `A20-98`，固件版本 `V00.00.00.07`，硬件版本 `V00.01`。
3. **通信流程：** 先扫描从设备 → 读取版本信息 → 关闭输出 → 再次扫描 → 读取内部参数 → 最终扫描。这是一个典型的设备初始化和状态检测流程。
4. **功能码 0x0301：** 该功能码未在标准文档中定义，推测为转接板特有的内部调试指令，用于读取 ADC 采样值或其他内部状态参数。
5. **重复操作：** 固件版本被读取了 2 次（seq=02, seq=03），扫描从设备被调用了 3 次（seq=01, seq=06, seq=0C），可能是为了确认通信稳定性和设备连接状态。
6. **协议网关功能（0x0400）：** 转接板支持 Modbus RTU 透传功能，PC 通过功能码 `0x0400` 发送完整的 Modbus RTU 报文，转接板将其转发给下游变送器。此时转接板充当 CPPI V3 ↔ Modbus RTU 的协议网关。
7. **多端口通信：** 前12条指令目标地址为 `0x000123`（端口35），第13条指令目标地址为 `0x000126`（端口38），说明转接板通过不同端口连接了不同的下游设备。
