# PS02 转接板 CPPI V3 指令解析（电压调试抓包）

> 基于一次新抓包数据（固件版本 **A20-98 V00.00.00.09**）逐条解析，协议帧格式参考 `指令解析 - 副本.md`。
> 所有帧均已通过 CRC8（头部校验）与 CRC16-CCITT-FALSE（数据校验）双重校验，字段边界准确。

---

## 一、协议基础（CPPI V3.1 帧格式）

| 字段 | 长度 | 说明 |
|------|------|------|
| 帧头 | 1B | 固定 `0x55` |
| 控制字段 | 1B | bit0: 1=发送帧/0=响应帧；bit[1:3]=001 表示 CPPI V3.1 |
| 目标地址 | 3B | 小端，设备ID << 8 + 端口号 |
| 源地址 | 3B | 小端，设备ID << 8 + 端口号 |
| 功能码 | 2B | 小端，决定指令功能 |
| 流水号 | 1B | 用于匹配请求/响应 |
| 数据长度 N | 2B | 小端，数据内容的字节数 |
| 头部校验 | 1B | CRC8，校验帧头到数据长度字段（共13字节），多项式 0x07，初始值 0x00 |
| 数据内容 | NB | 第一字节为响应错误码（仅响应帧），后续为实际数据 |
| 数据校验 | 2B | CRC16-CCITT-FALSE，校验数据段，多项式 0x1021，初始值 0xFFFF，不反射；存储为低字节在前（小端） |

**固定通信参数：**
- PC（主机）源地址：`0x112236`（36 22 11）
- 转接板（从机）目标地址：`0x000123`（23 01 00，端口 35）
- 下游变送器（经转接板转发）：`0x000126`（26 01 00，端口 38），Modbus 从机地址 `0x01`

**错误码定义：**

| 错误码 | 含义 |
|--------|------|
| 0x00 | 无错误 |
| 100 | CRC 校验错误 |
| 101 | 无此指令 |
| 102 | 当前状态不支持此操作 |
| 103 | 密码错误 |
| 104 | 参数格式错误 |
| 105 | 参数超范围 |
| 106 | 执行错误 |
| 107 | 参数错误 |

---

## 二、本抓包涉及的指令总览

| 流水号 | 功能码 | 指令名称 | 说明 |
|--------|--------|----------|------|
| 01 | `0x0300` | 扫描从设备 | 扫描参数 `01` |
| 02 | `0x0106` | 读取固件版本 | 返回 V00.00.00.09 |
| 03 | `0x0106` | 读取固件版本（重复） | 返回 V00.00.00.09 |
| 04 | `0x0108` | 读取硬件版本 | 返回 V00.01 |
| 05 | `0x0210` | 关闭所有输出 | 项目代号 `00` + 输出类型 `00` |
| 06 | `0x0300` | 扫描从设备 | 扫描参数 `00` |
| 07 | `0x0301` | 读取内部参数 | 返回参数值 `0x02` |
| 08 | `0x0301` | 读取内部参数 | 返回参数值 `0x02` |
| 09 | `0x0300` | 扫描从设备 | 扫描参数 `01` |
| 0A | `0x0400` | Modbus RTU 转发（启用 OWI） | 写寄存器 0x8000 = 0x0001 |

---

## 三、逐条指令解析

### 指令 1：扫描从设备（seq=01，功能码 0x0300）

| | 原始数据 |
|---|---|
| **发送** | `55 03 23 01 00 36 22 11 00 03 01 01 00 D1 01 D1 F1` |
| **接收** | `55 02 36 22 11 23 01 00 00 03 01 01 00 67 00 F0 E1` |

**发送帧字段：**

| 字段 | 值 | 说明 |
|------|-----|------|
| 控制字段 | `0x03` | 发送帧 + CPPI V3.1 |
| 目标地址 | `0x000123` | 转接板（端口 35） |
| 源地址 | `0x112236` | PC |
| 功能码 | `0x0300` | 扫描从设备 |
| 流水号 | `0x01` | 第 1 条 |
| 数据长度 | `0x0001` | 1 字节 |
| 头部校验 | `0xD1` | CRC8 |
| 数据内容 | `0x01` | 扫描参数 |
| 数据校验 | `D1 F1` | CRC16=0xF1D1 |

**接收帧字段：** 错误码 `0x00`（成功）。

**意义：** 扫描下游从设备接口类型，转接板响应成功。

---

### 指令 2：读取固件版本（seq=02，功能码 0x0106）

| | 原始数据 |
|---|---|
| **发送** | `55 03 23 01 00 36 22 11 06 01 02 00 00 1E FF FF` |
| **接收** | `55 02 36 22 11 23 01 00 06 01 02 14 00 AB 00 41 32 30 2D 39 38 20 56 30 30 2E 30 30 2E 30 30 2E 30 39 64 56` |

**接收帧字段：**

| 字段 | 值 | 说明 |
|------|-----|------|
| 功能码 | `0x0106` | 读取固件版本 |
| 流水号 | `0x02` | 匹配请求 |
| 数据长度 | `0x0014` | 20 字节 |
| 错误码 | `0x00` | 成功 |
| 版本字符串 | `A20-98 V00.00.00.09` | ASCII 解码（19 字节） |
| 数据校验 | `64 56` | CRC16=0x5664 |

> **与副本.md 的差异：** 固件版本由 `V00.00.00.07` 升级为 `V00.00.00.09`。

---

### 指令 3：读取固件版本（重复验证，seq=03）

| | 原始数据 |
|---|---|
| **发送** | `55 03 23 01 00 36 22 11 06 01 03 00 00 75 FF FF` |
| **接收** | `55 02 36 22 11 23 01 00 06 01 03 14 00 C0 00 41 32 30 2D 39 38 20 56 30 30 2E 30 30 2E 30 30 2E 30 39 64 56` |

**解析：** 与指令 2 相同功能码 `0x0106`，流水号 `0x03`，返回相同固件版本 `A20-98 V00.00.00.09`。

---

### 指令 4：读取硬件版本（seq=04，功能码 0x0108）

| | 原始数据 |
|---|---|
| **发送** | `55 03 23 01 00 36 22 11 08 01 04 00 00 31 FF FF` |
| **接收** | `55 02 36 22 11 23 01 00 08 01 04 0E 00 51 00 41 32 30 2D 39 38 20 56 30 30 2E 30 31 CE C4` |

**接收帧字段：**

| 字段 | 值 | 说明 |
|------|-----|------|
| 功能码 | `0x0108` | 读取硬件版本 |
| 数据长度 | `0x000E` | 14 字节 |
| 错误码 | `0x00` | 成功 |
| 版本字符串 | `A20-98 V00.01` | ASCII 解码（13 字节） |
| 数据校验 | `CE C4` | CRC16=0xC4CE |

---

### 指令 5：关闭所有输出（seq=05，功能码 0x0210）

| | 原始数据 |
|---|---|
| **发送** | `55 03 23 01 00 36 22 11 10 02 05 02 00 61 00 00 0F 1D` |
| **接收** | `55 02 36 22 11 23 01 00 10 02 05 01 00 E8 00 F0 E1` |

**发送帧字段：**

| 字段 | 值 | 说明 |
|------|-----|------|
| 功能码 | `0x0210` | 设定输出项目 |
| 数据长度 | `0x0002` | 2 字节 |
| 数据内容 | `00 00` | 项目代号 `0x00`（关闭所有档位）+ 输出类型 `0x00`（输出零点） |
| 数据校验 | `0F 1D` | CRC16=0x1D0F |

**接收：** 错误码 `0x00`（成功）。

**意义：** 关闭转接板所有电流/电压输出。

---

### 指令 6：扫描从设备（seq=06，功能码 0x0300）

| | 原始数据 |
|---|---|
| **发送** | `55 03 23 01 00 36 22 11 00 03 06 01 00 C7 00 F0 E1` |
| **接收** | `55 02 36 22 11 23 01 00 00 03 06 01 00 71 00 F0 E1` |

**解析：** 功能码 `0x0300`，流水号 `0x06`，扫描参数 `0x00`。响应错误码 `0x00`（成功）。

---

### 指令 7：读取内部参数（seq=07，功能码 0x0301）

| | 原始数据 |
|---|---|
| **发送** | `55 03 23 01 00 36 22 11 01 03 07 00 00 DB FF FF` |
| **接收** | `55 02 36 22 11 23 01 00 01 03 07 02 00 47 00 02 4D 3D` |

**接收帧字段：**

| 字段 | 值 | 说明 |
|------|-----|------|
| 功能码 | `0x0301` | 读取内部参数 |
| 数据长度 | `0x0002` | 2 字节 |
| 头部校验 | `0x47` | CRC8 |
| 数据内容 | `00 02` | 错误码 `0x00` + 参数值 `0x02` |
| 数据校验 | `4D 3D` | CRC16=0x3D4D |

> **纠偏：** 副本.md 将 CRC8 字段 `0x47` 误当作参数值，实际参数值为 `0x02`（CRC8 已单独校验通过）。

---

### 指令 8：读取内部参数（seq=08，功能码 0x0301）

| | 原始数据 |
|---|---|
| **发送** | `55 03 23 01 00 36 22 11 01 03 08 00 00 9C FF FF` |
| **接收** | `55 02 36 22 11 23 01 00 01 03 08 02 00 00 00 02 4D 3D` |

**解析：** 功能码 `0x0301`，流水号 `0x08`。响应数据 `00 02`，参数值 `0x02`（与指令 7 相同）。

---

### 指令 9：扫描从设备（seq=09，功能码 0x0300）

| | 原始数据 |
|---|---|
| **发送** | `55 03 23 01 00 36 22 11 00 03 09 01 00 80 01 D1 F1` |
| **接收** | `55 02 36 22 11 23 01 00 00 03 09 01 00 36 00 F0 E1` |

**解析：** 功能码 `0x0300`，流水号 `0x09`，扫描参数 `0x01`。响应错误码 `0x00`（成功）。

---

### 指令 10：Modbus RTU 转发 — 启用 OWI 通信模式（seq=0A，功能码 0x0400）

| | 原始数据 |
|---|---|
| **发送** | `55 03 26 01 00 36 22 11 00 04 0A 0B 00 BE 01 29 80 00 00 01 02 00 01 25 26 53 24` |
| **接收** | `55 02 36 22 11 26 01 00 00 04 0A 09 00 1E 00 01 29 80 00 00 01 B4 0C CE EA` |

**发送帧字段：**

| 字段 | 值 | 说明 |
|------|-----|------|
| 目标地址 | `0x000126` | 设备ID=0x0001，端口号=0x26(38) |
| 源地址 | `0x112236` | PC |
| 功能码 | `0x0400` | Modbus RTU 转发 |
| 流水号 | `0x0A` | 第 10 条 |
| 数据长度 | `0x000B` | 11 字节 |
| 头部校验 | `0xBE` | CRC8 |
| 数据校验 | `53 24` | CRC16=0x2453 |

**发送数据内容（Modbus RTU 报文）：**

| 字段 | 值 | 说明 |
|------|-----|------|
| Modbus 从机地址 | `0x01` | 下游变送器 |
| Modbus 功能码 | `0x29` | F41 写寄存器 |
| 寄存器地址 | `0x8000` | OWI 通信使能 |
| 寄存器数量 | `0x0001` | 1 个 |
| 数据长度 | `0x02` | 2 字节 |
| 寄存器值 | `0x0001` | 启用 OWI 通信模式 |
| CRC16 | `25 26` | Modbus 校验 |

**接收数据内容：**

| 字段 | 值 | 说明 |
|------|-----|------|
| CPPI 错误码 | `0x00` | 成功 |
| Modbus 从机地址 | `0x01` | 变送器 |
| Modbus 功能码 | `0x29` | F41 写寄存器 |
| 寄存器地址 | `0x8000` | OWI 通信使能 |
| 寄存器数量 | `0x0001` | 1 个 |
| Modbus CRC16 | `B4 0C` | Modbus 校验 |

**意义：** PC 经转接板（端口 38）以 Modbus RTU 协议向下游变送器写寄存器 0x8000 = 0x0001，**启用 OWI 通信模式**，变送器响应成功。

---

## 四、通信时序总结

```
PC (0x112236)                                 转接板 (0x000123, 端口35)         变送器(0x000126, 端口38)
    |                                                  |                              |
    |-- 扫描从设备 (0x0300, seq=01) -------------------->|                              |
    |<-- 响应成功 ---------------------------------------|                              |
    |-- 读固件版本 (0x0106, seq=02) -------------------->|                              |
    |<-- A20-98 V00.00.00.09 ----------------------------|                              |
    |-- 读固件版本 (0x0106, seq=03) -------------------->|                              |
    |<-- A20-98 V00.00.00.09 (重复) ----------------------|                              |
    |-- 读硬件版本 (0x0108, seq=04) -------------------->|                              |
    |<-- A20-98 V00.01 ----------------------------------|                              |
    |-- 关闭所有输出 (0x0210, seq=05) ------------------>|                              |
    |<-- 响应成功 ---------------------------------------|                              |
    |-- 扫描从设备 (0x0300, seq=06) -------------------->|                              |
    |<-- 响应成功 ---------------------------------------|                              |
    |-- 读内部参数 (0x0301, seq=07) -------------------->|                              |
    |<-- 参数值 0x02 -------------------------------------|                              |
    |-- 读内部参数 (0x0301, seq=08) -------------------->|                              |
    |<-- 参数值 0x02 -------------------------------------|                              |
    |-- 扫描从设备 (0x0300, seq=09) -------------------->|                              |
    |<-- 响应成功 ---------------------------------------|                              |
    |-- Modbus转发: 启用OWI (0x0400, seq=0A) ----------->|-------------------------->|
    |<-- 变送器写入成功 ---------------------------------|---------------------------|
```

---

## 五、关键发现

1. **固件版本升级：** 本抓包转接板固件为 `A20-98 V00.00.00.09`（副本.md 为 `V00.00.00.07`），硬件版本仍为 `A20-98 V00.01`。
2. **0x0301 参数值纠偏：** 副本.md 把响应帧的 CRC8 字段（0x47/0x00/0x6B…）误当作参数值；经 CRC8+CRC16 双重校验，实际响应数据为 `00 02`，参数值为 `0x02`。
3. **扫描参数有 0/1 两种取值：** seq=01、09 扫描参数为 `0x01`，seq=06 为 `0x00`，含义待进一步确认。
4. **初始化流程稳定：** 扫描从设备 → 读版本 → 关闭输出 → 再扫描 → 读内部参数 → 再扫描 → 启用 OWI，与副本.md 的初始化序列一致，仅 0x0301 组数由 5 组减少为 2 组。
5. **协议网关（0x0400）：** 转接板支持 Modbus RTU 透传，PC 经端口 38 向下游变送器写寄存器，转接板充当 CPPI V3 ↔ Modbus RTU 网关。
6. **CRC 校验通过：** 全部帧 CRC8（头部）与 CRC16-CCITT-FALSE（数据）均校验一致，字段边界与多字节大小端解析无误。
